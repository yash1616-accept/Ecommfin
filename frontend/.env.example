# ============================================================
# ShopEase Frontend - Environment Variables
# ============================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# IMPORTANT: Never commit your .env file to version control!
#
# NOTE: All frontend env variables MUST start with VITE_
#       to be accessible in the browser via import.meta.env
# ============================================================


# ──────────────────────────────────────────────────────────────
# API CONFIGURATION
# ──────────────────────────────────────────────────────────────

# Backend API base URL
# Development: http://localhost:3000/api (local backend server)
# Production:  Your deployed backend URL (Vercel, AWS App Runner, etc.)
#
# Examples:
#   Local:         http://localhost:3000/api
#   Vercel:        https://shopease-api.vercel.app/api
#   AWS App Runner: https://your-backend-id.us-east-1.awsapprunner.com/api
VITE_API_URL=http://localhost:3000/api


# ──────────────────────────────────────────────────────────────
# IMPORTANT NOTES
# ──────────────────────────────────────────────────────────────
#
# 1. VITE_ prefix is REQUIRED for all frontend environment variables.
#    Without it, the variable will NOT be available in browser code.
#
# 2. Changes to .env require a restart of the Vite dev server.
#
# 3. In production (Vercel/AWS), set VITE_API_URL as an environment
#    variable in the platform's dashboard BEFORE building. Vite
#    embeds env values at BUILD TIME, not at runtime.
#
# 4. For Vercel: Set in Dashboard → Settings → Environment Variables
#    For AWS App Runner: Set in Service → Configuration → Environment Variables
