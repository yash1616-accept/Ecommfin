# ============================================================
# ShopEase Backend - Environment Variables
# ============================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# IMPORTANT: Never commit your .env file to version control!
# ============================================================


# ──────────────────────────────────────────────────────────────
# SERVER CONFIGURATION
# ──────────────────────────────────────────────────────────────

# Port the Express server listens on
# Development: 3000 (default)
# Production:  Set by hosting platform (Vercel, AWS App Runner, etc.)
PORT=3000

# Node environment
# Development: development (default)
# Production:  production (set automatically by hosting platforms)
NODE_ENV=development


# ──────────────────────────────────────────────────────────────
# DATABASE CONFIGURATION
# ──────────────────────────────────────────────────────────────

# Database type: "sqlite" for local development, "postgresql" for production
# Development: sqlite   → uses local file (prisma/dev.db), no external DB needed
# Production:  postgresql → requires a PostgreSQL connection string
DATABASE_TYPE=postgresql

# PostgreSQL connection string
# Development: Use a local PostgreSQL instance or a free Supabase project
# Production:  Use your Supabase/AWS RDS/Neon connection string
#
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#
# Examples:
#   Local:      postgresql://postgres:password@localhost:5432/shopease
#   Supabase:   postgresql://postgres.xxxxx:password@aws-1-ap-south-1.pooler.supabase.com:5432/postgres
#   Neon:       postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/dbname
DATABASE_URL=postgresql://postgres:password@localhost:5432/shopease


# ──────────────────────────────────────────────────────────────
# CORS CONFIGURATION (Optional)
# ──────────────────────────────────────────────────────────────

# Allowed origins for CORS (comma-separated)
# Development: http://localhost:5173 (Vite dev server)
# Production:  Your frontend deployment URL(s)
# CORS_ORIGIN=http://localhost:5173
